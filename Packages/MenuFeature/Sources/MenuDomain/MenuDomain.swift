/// Public entry point for the product menu feature's domain layer.
public enum MenuDomain {
    /// Stable module marker used by package and import contract tests.
    public static let moduleName = "MenuDomain"
}
