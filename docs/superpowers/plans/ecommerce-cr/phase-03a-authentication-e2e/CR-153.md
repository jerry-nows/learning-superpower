# CR-153

**Objective:** Supply auth configuration to the local API container.

**Files:** `Infrastructure/compose.yaml`

**Steps:**
1. Add a Compose contract for JWT issuer/audience, signing secret and seed configuration.
2. Pass secrets only through environment interpolation, never image layers or command arguments.
3. Add an explicit migration/seed startup dependency without exposing database ports.
4. Validate Compose with substituted non-secret fixtures.

**Dependencies:** CR-141, CR-143, CR-161

**Duration:** 10m

**Definition of Done:** Local API starts migrated and can authenticate the configured seeded user.
