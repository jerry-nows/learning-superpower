# CR-142

**Objective:** Compose health and auth HTTP routes.

**Files:** `Backend/internal/platform/httpapi/router.go`

**Steps:**
1. Write a router contract for health, login, refresh and logout routes.
2. Accept the auth handler through an interface rather than constructing repositories.
3. Apply JSON content type, body limits and method-specific routing.
4. Verify unknown routes and methods return stable non-sensitive responses.

**Dependencies:** CR-046, CR-137, CR-139

**Duration:** 15m

**Definition of Done:** `/v1/auth/login`, `/refresh` and `/logout` are reachable through one composition router.
