# CR-134

**Objective:** Pin backend authentication and integration-test dependencies.

**Files:** `Backend/go.mod`

**Steps:**
1. Add a module contract for pgx, go-redis, goose, JWT, x/crypto, Testify and testcontainers.
2. Confirm it fails against the dependency-free module.
3. Add exact compatible versions and run `go mod tidy`.
4. Run `govulncheck`, `go mod verify` and inspect `go.sum` for unexpected modules.

**Dependencies:** CR-011

**Duration:** 10m

**Definition of Done:** Backend dependencies are pinned, verified and vulnerability-scanned.
