# CR-156

**Objective:** Implement the local authentication smoke journey.

**Files:** `Infrastructure/scripts/auth-e2e.sh`

**Steps:**
1. Add a failing shell contract for login, refresh, reuse detection and logout.
2. Read demo credentials from environment and keep responses in permission-restricted temporary files.
3. Assert old refresh-token reuse revokes the family and logout rejects further refresh.
4. Delete temporary credentials/tokens through a trap and redact failure output.

**Dependencies:** CR-145, CR-154, CR-155

**Duration:** 15m

**Definition of Done:** One local command proves the real TLS API auth lifecycle without leaking secrets.
