# CR-143

**Objective:** Wire production auth dependencies into the API process.

**Files:** `Backend/cmd/api/main.go`

**Steps:**
1. Add a startup test seam for environment parsing and dependency construction.
2. Open PostgreSQL and Redis with bounded startup pings and apply migrations.
3. Construct repositories, password verifier, JWT issuer, refresh generator, service, handler and router.
4. Close resources on shutdown and fail startup without printing DSNs or secrets.

**Dependencies:** CR-135, CR-142

**Duration:** 15m

**Definition of Done:** The local API serves working auth endpoints backed by PostgreSQL and Redis.
