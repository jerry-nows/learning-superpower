# CR-149

**Objective:** Test LoginData against mocked HTTP and Keychain boundaries.

**Files:** `Packages/LoginFeature/Tests/LoginDataTests/AuthRepositoryTests.swift`

**Steps:**
1. Intercept login, refresh and logout traffic with Mocker.
2. Assert successful login maps the user and atomically saves both tokens.
3. Assert server errors map to stable domain errors and malformed payloads fail closed.
4. Assert terminal refresh/logout clears secrets and concurrent refresh uses one remote call.

**Dependencies:** CR-133, CR-148

**Duration:** 15m

**Definition of Done:** Deterministic integration tests cover LoginData mapping and credential persistence.
