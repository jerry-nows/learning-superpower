# CR-161

**Objective:** Package API, seed command and migrations in the backend image.

**Files:** `Backend/Dockerfile`

**Steps:**
1. Add an image contract requiring separate `/api` and `/seed` binaries plus `/migrations` assets.
2. Build both commands in the existing non-CGO build stage.
3. Copy binaries and migrations into the non-root runtime without shells or build tools.
4. Build the image and verify both entrypoints run as UID 10001.

**Dependencies:** CR-037, CR-141, CR-143

**Duration:** 10m

**Definition of Done:** Compose can run migrations through the API and invoke an idempotent seed command from one minimal image.
