# CR-129

**Objective:** Declare the shared Security package.

**Files:** `Packages/Security/Package.swift`

**Steps:**
1. Add a manifest contract asserting iOS 17, Swift 6, `Security` and `SecurityTests` targets.
2. Run the contract and confirm it fails because the manifest is absent.
3. Add the package without feature-module dependencies.
4. Resolve the package and verify no secret-bearing generated file is tracked.

**Dependencies:** CR-021

**Duration:** 10m

**Definition of Done:** Security resolves independently and exposes only Keychain/biometric abstractions.
