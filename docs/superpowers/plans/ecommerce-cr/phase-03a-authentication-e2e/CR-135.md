# CR-135

**Objective:** Implement an embedded PostgreSQL migration runner.

**Files:** `Backend/internal/platform/migration/runner.go`

**Steps:**
1. Write the CR-136 failing test against `RunUp` and `RunDown`.
2. Implement a runner over `database/sql` with a caller-supplied migration filesystem.
3. Make startup migration errors fail closed and preserve the underlying cause.
4. Run focused tests and `go vet`.

**Dependencies:** CR-037, CR-134

**Duration:** 15m

**Definition of Done:** The API can apply and roll back versioned migrations deterministically.
