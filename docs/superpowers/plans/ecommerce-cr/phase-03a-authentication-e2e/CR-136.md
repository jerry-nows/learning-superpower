# CR-136

**Objective:** Verify auth migration up/down on PostgreSQL.

**Files:** `Backend/internal/platform/migration/runner_test.go`

**Steps:**
1. Start PostgreSQL with testcontainers and wait for readiness.
2. Apply `000001_auth.sql`, assert the users schema and indexes, and prove no refresh-family state is persisted in PostgreSQL.
3. Roll back and assert the auth objects are removed.
4. Reapply to prove deterministic recovery and run with `-race`.

**Dependencies:** CR-135

**Duration:** 15m

**Definition of Done:** Real PostgreSQL proves migration up, down and reapply behavior.
