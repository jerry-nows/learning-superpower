# CR-119

**Objective:** Add a reusable workflow exposing the strict PR policy as a stable required check.

**Files:** `.github/workflows/pr-policy.yml`, `Infrastructure/ci/tests/validate-workflows_test.sh`

**Steps:**
1. Add structural assertions for callable inputs, minimal permissions, and the `PR Policy` job name.
2. Invoke the GitFlow validator on Ubuntu with immutable action pins.
3. Support equivalent manual-dispatch inputs and run local workflow validation.

**Duration:** 15m

**Dependencies:** CR-114, CR-118

**Parallel:** No

**Definition of Done:** Reusable and manual workflow invocations produce a stable `PR Policy` check using the approved validator.
