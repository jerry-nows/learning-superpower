# CR-117

**Objective:** Add a fail-closed preflight for the optional local Docker SonarQube service.

**Files:** `Infrastructure/ci/preflight-sonarqube.sh`, `Infrastructure/ci/tests/preflight-sonarqube_test.sh`

**Steps:**
1. Test missing URL, missing token, unreachable, starting, and healthy server states.
2. Require credentials and accept only an `UP` system status without printing secrets.
3. Run the contract and verify the token fixture is absent from diagnostics.

**Duration:** 15m

**Dependencies:** CR-115

**Parallel:** Yes

**Definition of Done:** Optional local preflight exits `0` only for an authenticated, reachable `UP` server and never exposes the token; cloud CI does not invoke it.
