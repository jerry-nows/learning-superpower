# CR-128

**Objective:** Publish the CI implementation and configure strict GitHub repository gates.

**Files:** No repository file changes expected; GitHub branches, the SonarQube Cloud secret/project, rulesets, and PR #3 are configured through authenticated administration interfaces.

**Steps:**
1. Run all local CI targets and confirm a clean worktree before publishing the feature branch.
2. Create `develop`, verify `SONAR_TOKEN` presence and the combined SonarQube Cloud project identity; never register a personal runner.
3. Observe exact successful check contexts, install or verify Sourcery review, and apply strict `main` and `develop` rulesets without weakening gates.

**Duration:** 15m

**Dependencies:** CR-127

**Parallel:** No

**Definition of Done:** `develop` exists; required SonarQube Cloud configuration is present; PR #3 shows successful PR Policy, Backend, hosted iOS, Security, SonarQube, Quality Gate, and Sourcery evidence; strict protected-branch rulesets require those checks.
