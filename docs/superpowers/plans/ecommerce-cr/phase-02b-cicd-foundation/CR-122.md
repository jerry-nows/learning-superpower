# CR-122

**Objective:** Add reusable source, dependency, secret, container, and SBOM security scanning.

**Files:** `.github/workflows/security.yml`, `Infrastructure/ci/tests/validate-workflows_test.sh`

**Steps:**
1. Assert CodeQL, dependency review, Gitleaks, Trivy, SBOM, permissions, and immutable action pins.
2. Separate scan jobs and restrict dependency review to pull requests.
3. Fail on high or critical findings and retain sanitized SARIF and CycloneDX artifacts for seven days.

**Duration:** 15m

**Dependencies:** CR-118

**Parallel:** Yes

**Definition of Done:** The stable `Security` checks fail on prohibited findings and publish sanitized SARIF and `sbom.cdx.json` evidence.
