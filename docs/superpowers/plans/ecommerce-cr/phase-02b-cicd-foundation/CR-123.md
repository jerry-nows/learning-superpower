# CR-123

**Objective:** Configure SonarQube source, test, exclusion, and coverage mappings.

**Files:** `sonar-project.properties`, `Infrastructure/ci/tests/sonar_config_test.sh`

**Steps:**
1. Assert project metadata, UTF-8, roots, exclusions, and Go and Swift coverage paths.
2. Add `sonar.organization=jerry-nows` and `sonar.projectKey=jerry-nows_learning-superpower` for the combined repository quality unit without embedded credentials or URL.
3. Run the mapping and secret contract.

**Duration:** 15m

**Dependencies:** CR-117

**Parallel:** Yes

**Definition of Done:** SonarQube for OSS maps Go and Swift sources, tests, generated exclusions, and both coverage reports into the combined public-repository project without checked-in connection secrets.
