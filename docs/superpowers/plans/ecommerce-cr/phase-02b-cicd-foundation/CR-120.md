# CR-120

**Objective:** Add a reusable backend formatting, analysis, race-test, and coverage workflow.

**Files:** `.github/workflows/backend.yml`, `Infrastructure/ci/tests/validate-workflows_test.sh`

**Steps:**
1. Assert the callable workflow, runner, Go quality commands, and coverage output contract.
2. Pin actions and staticcheck, then run checks from `Backend/`.
3. Verify Go checks locally and validate workflow structure.

**Duration:** 15m

**Dependencies:** CR-118

**Parallel:** Yes

**Definition of Done:** The stable `Backend` check passes formatting, vet, static analysis, race tests, and publishes `Backend/coverage.out`.
