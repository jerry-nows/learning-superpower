# CR-125

**Objective:** Orchestrate all reusable CI gates behind one stable aggregate result.

**Files:** `.github/workflows/ci.yml`, `Infrastructure/ci/tests/validate-workflows_test.sh`

**Steps:**
1. Assert protected-branch PR and push triggers, manual dispatch, concurrency, and all reusable calls.
2. Pass PR metadata to policy and invoke backend, iOS, security, and SonarQube workflows.
3. Add an always-running final job that requires every dependency result to equal `success`.

**Duration:** 15m

**Dependencies:** CR-119, CR-120, CR-121, CR-122, CR-124

**Parallel:** No

**Definition of Done:** PRs, protected pushes, and manual runs expose `Quality Gate`, which succeeds only when every required reusable check succeeds.
