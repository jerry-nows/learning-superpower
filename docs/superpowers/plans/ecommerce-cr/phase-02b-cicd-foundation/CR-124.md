# CR-124

**Objective:** Add a reusable fail-closed SonarQube analysis and Quality Gate workflow.

**Files:** `.github/workflows/quality.yml`, `Infrastructure/ci/tests/validate-workflows_test.sh`

**Steps:**
1. Assert runner labels, preflight, value mapping, scanner invocation, and synchronous Quality Gate waiting.
2. Generate Go and Swift coverage, run preflight, and invoke the immutably pinned scanner.
3. Pass the token only through action environment and validate against a healthy local server.

**Duration:** 15m

**Dependencies:** CR-117, CR-118, CR-123

**Parallel:** No

**Definition of Done:** The stable `SonarQube` check fails closed unless analysis completes and the Quality Gate passes without exposing credentials.
