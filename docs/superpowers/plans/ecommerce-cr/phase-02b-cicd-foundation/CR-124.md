# CR-124

**Objective:** Add a reusable fail-closed SonarQube for OSS analysis and Quality Gate workflow.

**Files:** `.github/workflows/quality.yml`, `Infrastructure/ci/tests/validate-workflows_test.sh`

**Steps:**
1. Assert hosted macOS, explicit Xcode selection, project identity, scanner invocation, synchronous Quality Gate waiting, and fork safety.
2. Generate Go and Swift coverage and invoke the immutably pinned SonarQube for OSS scanner.
3. Pass only `SONAR_TOKEN`, fail trusted runs when it is absent, and fail fork pull requests explicitly without executing privileged untrusted code.

**Duration:** 15m

**Dependencies:** CR-117, CR-118, CR-123

**Parallel:** No

**Definition of Done:** The stable `SonarQube` check fails closed unless trusted SonarQube for OSS analysis completes and its Quality Gate passes without exposing credentials.
